# Бригада — учёт заказов и смен монтажной бригады

## Что это
Telegram Mini App (и обычная веб-страница) для бригадира: ведёт заказы,
работы внутри заказов (монтаж/демонтаж/транспорт/склад), суммы каждому
участнику, импортирует план из выгрузки 1С и формирует текстовый отчёт для HR.

## Жёсткие правила (не нарушать без явной просьбы владельца)
1. **Один файл.** Всё приложение — `index.html` (HTML+CSS+JS, без сборки,
   без фреймворков). Внешние зависимости только с CDN: SheetJS (cdnjs),
   telegram-web-app.js, Google Fonts.
2. **Формат отчёта для HR неприкосновенен.** Функция `buildReport` выдаёт:

   ```
   Заказ 963

   Монтаж 14.05.26 15:00-18:00
   Петров Алексей 7000
   Хасанов Амир 5000

   Транспорт Монтаж и Демонтаж
   Садыгов Ислам 6000
   ```

   Шапка работы: `тип [примечание] [дата ДД.ММ.ГГ] [ЧЧ:ММ-ЧЧ:ММ]`,
   далее строки `Имя Фамилия сумма` (числа без пробелов-разделителей).
   Этот текст сверяет HR заказчика — менять формат нельзя.
3. **Хранилище.** Порядок бэкендов в `loadData/persist`:
   Telegram CloudStorage (чанки по 3500 символов, ключи `brigada-v2:N` +
   `brigada-v2:n`) → `window.storage` (артефакт Claude) → память.
   `localStorage` не использовать. При изменении схемы данных писать
   миграцию в блоке старта (`(async () => { ... })()`).
4. **Русский интерфейс**, крупные кнопки (min-height 48px), телефон — основной экран.
5. **Касса бригадира — открытая договорённость бригады** (все знают, бригадир
   показывает цифры по запросу). Реализована как две суммы на строке (по отчёту /
   на руки). Отчёт HR всегда берёт сумму по отчёту (`amount`), не `cash`.

## Модель данных (объект D)
```js
{
  people: [{ id, name, rate }],            // rate — обычная ставка за смену
  types:  ['Монтаж','Демонтаж','Транспорт','Склад', ...пользовательские],
  orders: [{ id, num, works: [
    { id, planId|null, type, note, date|null, from, to,
      lines: [{ personId, name, amount, cash }] } // amount — по отчёту (в HR); cash — на руки
  ]}],
  plan:   [{ id, date, from, to|null, op, client, order }] // импорт из 1С
}
```
- Деньги: заработок человека «по отчёту» = сумма его `amount`. В HR всегда идёт `amount`.
  Никаких ставок «за час» — только сумма за работу.
- **Касса бригадира (взаиморасчёты).** У строки две суммы: `amount` (по отчёту, в HR)
  и `cash` (выдано наличными на руки). При добавлении человека `cash = amount`
  (ставка); бригадир правит вручную. Касса = Σ(amount − cash) по всем работам.
  Это ОТКРЫТАЯ договорённость бригады (не обман): бригадир закрывает наличными
  сразу, официальная выплата возвращается ему. Хелперы: lineCash, workCash,
  workKassa, orderCash, orderKassa. Касса показывается на главной (за месяц),
  в списке заказов (chip), внутри заказа (блок + разбивка по людям) и в сводке
  (плашка + «отчёт → на руки» по каждому). Отчёт HR при этом НЕ меняется.
- Один человек может быть в нескольких заказах в один день — это норма,
  в ленте дня показывается предупреждение-подсказка (не запрет).
- Работы без даты в месячной сводке относятся к месяцу, где у их заказа
  есть датированные работы.

## Импорт 1С (parsePlanWorkbook)
Выгрузка «Календарь операций» (.XLS, лист TDSheet): строки-дни
(`Четверг ... 2 июля 2026 г.`), строки-события (колонка 0 — время
`ЧЧ : ММ\nЧЧ : ММ`, дальше операция с клиентом, примечание `заказ №NNNN`).
При импорте план в затронутом диапазоне дат заменяется, вне его — сохраняется.
`guessType`: доставка/транспортировка/вывоз упаковки → Транспорт;
демонтаж → Демонтаж; монтаж → Монтаж; погрузка/отгрузка/склад/принять → Склад.
ВАЖНО: в план импортируются ТОЛЬКО события с типом Монтаж или Демонтаж —
транспорт и склад отсеиваются в конце parsePlanWorkbook (решение владельца).

## Дизайн-токены (светлая, Notion-вдохновлённая, палитра владельца)
bg #F3F3F4, card #FFFFFF, card2 #F1F0EE, line #DCDCDD/#C5C3C6,
ink #14110F, dim #7E7F83; действие — бирюза #1985A1 (кнопки, активная вкладка);
дополнительные: slate #4C5C68, sand #D9C5B2 (тёмный #9C7B54), brown #34312D.
Типы: Монтаж #1985A1, Демонтаж #4C5C68, Транспорт #7E7F83, Склад #9C7B54,
свои #34312D. Шрифты: Lora (крупные заголовки, большие цифры), Inter (текст).
Элементы стиля: заголовок страницы с эмодзи, серые UPPERCASE секции с нижней
линией, callout-блоки (серый фон, эмодзи), отчёт как code-block.
Главная — дашборд: приветствие, месяц в цифрах, быстрые действия,
«Сегодня» (факт + неоформленный план), последние заказы.

## Проверка перед коммитом
- Открыть index.html в браузере: вкладки переключаются, заказ создаётся,
  работа сохраняется, отчёт копируется.
- Прогнать импорт тестовой выгрузки 1С, если менялся парсер.
- Убедиться, что данные переживают перезагрузку страницы.

## Деплой
GitHub Pages из ветки main (корень). URL страницы прописан в @BotFather
как Web App — после `git push` изменения появляются в Telegram через
~1–2 минуты без дополнительных действий.
